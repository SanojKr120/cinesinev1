# ===========================================
# CineSine Backend - Environment Variables
# ===========================================
# Copy this file to .env and fill in your values

# Server
PORT=5000
NODE_ENV=development

# MongoDB Connection String
# Get this from MongoDB Atlas: https://cloud.mongodb.com/
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/cinesine?retryWrites=true&w=majority

# Frontend URL (for CORS)
# Local development
FRONTEND_URL=http://localhost:5173
# Production (uncomment when deploying)
# FRONTEND_URL=https://your-frontend-domain.vercel.app

# Email Configuration (for Contact Form)
# For Custom Domain Email (like info@yourdomain.com):
EMAIL_USERNAME=your-email@yourdomain.com
EMAIL_PASSWORD=your-email-password
SMTP_HOST=mail.yourdomain.com
SMTP_PORT=587
SMTP_SECURE=false

# Common SMTP Hosts:
# - Zoho: smtp.zoho.com (or smtp.zoho.in for India)
# - Hostinger: smtp.hostinger.com
# - GoDaddy: smtpout.secureserver.net
# - cPanel: mail.yourdomain.com
# - Gmail: smtp.gmail.com (use App Password)
